#include <iostream>
using namespace std;
int main()
{

    // int a,b;
    // cin>>a>>b;
    // if(a>b){
    //     cout<<"a "<<a;
    // }
    // else if (b>a)
    // {
    //     cout<<"b "<<b;
    // }
    // else
    // {
    //  cout<<"both are equal";
    // }

    /* int n;
     n=cin.get();// Gives ascii value
     cout<<"value of n is "<<n;
  */
    /*
    int b;
    int a=24;
    if(a==24)
    {
        cout<<"Positive";
    }
    if(a>0)
    {
        cout<<"love";
    }
    else{
        cout<<"nothing";
    }
    */
    /*
    char ch;
    cin>>ch;
    if(ch>='A'&& ch<='Z')
    {
        cout<<"Upper case";
    }
    else if(ch>='a'&&ch<='z')
    {
        cout<<"lower case";
    }
    else{
        cout<<"the number is numeric";
    }
    */
    /*
    int i;
    cout<<"Enter the number you want the table of ";
    cin>>i;
    int n=1;
    while(n<=i)
    {
        cout<<i*n<<endl;
        n++;
    }
    */
    /* int n = 1, sum = 0;
     int i;
     cin >> i;
     while (n <= i)
     {
         sum = sum + n;
         // cout << sum<<endl;
         n++;
     }
     cout << sum;
 */
    // Adding only even numbers
   /* int n = 2, sum = 0;
    int i;
    cin >> i;
    while (n <= i)
    {
        sum = sum + n;
        // cout << sum<<endl;
        n+=2;
    }
    cout << sum;
    */
   /*
   int n,i=2;
   cout<<"enter the number you wanna check"<<endl;
   cin>>n;
   while(i<n)
   {
    if(n%i==0)
    {
       cout<<"It is not prime"; 
    }
    else
    {
        cout<<"it is not prime";
    }
    i++;
   }
   */

}